//两个节点的最近公共祖先，就是这两个点的公共祖先里面，离根最远的那个。
/* 为了方便，我们记某点集 S={v1,v2,.....,vn} 的最近公共祖先为 LCA(v1,v2,.....,vn) 或 LCA(S)*/

/*
性质
    01 LCA({u}) = u;

    02 u 是 v 的祖先，当且仅当 LCA(u, v) = u;

    03 如果 u 不为 v 的祖先并且 v 不为 u 的祖先，那么 u,v 分别处于 LCA(u,v) 的两棵不同子树中；

    04 前序遍历中，LCA(S) 出现在所有 S 中元素之前，后序遍历中 LCA(S) 则出现在所有 S 中元素之后；

    0 两点集并的最近公共祖先为两点集分别的最近公共祖先的最近公共祖先，
        即 LCA(A\cup B)=\text{LCA}(\text{LCA}(A), \text{LCA}(B))；
两点的最近公共祖先必定处在树上两点间的最短路上；
d(u,v)=h(u)+h(v)-2h(\text{LCA}(u,v))，其中 d 是树上两点间的距离，h 代表某点到树根的距离。
*/